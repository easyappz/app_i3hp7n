import React from 'react';
import { Typography, Box } from '@mui/material';

const Search = () => {
  return (
    <Box>
      <Typography variant="h4">Поиск</Typography>
      {/* Здесь будет поиск пользователей и контента */}
    </Box>
  );
};

export default Search;
